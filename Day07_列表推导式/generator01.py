# 一边循环一边计算的机制，称为生成器:generator'''得到生成器的方式：    1、通过列表推导式得到生成器'''# [x for x in range(1000000)]# [0, 3, 6, 9..., 27]newlist = [x * 3 for x in range(10)]print(newlist)# 得到生成器  将[]换成()g = (x * 3 for x in range(10))print(type(g))  # <class 'generator'># 方式1 通过调用__next__()方式得到元素print(g.__next__())print(g.__next__())print(g.__next__())print(g.__next__())# 方式2：next(生成器对象) builtins 系统内置函数# 每调用一次，next()则会产生一个元素print(next(g))print(next(g))print(next(g))print(next(g))print(next(g))  #StopIteration 生成器产生的元素超过了。