# 装饰器带参数就相当于3层嵌套。最外层的函数接收装饰器的参数，外层函数里面的内容是原来的装饰器的内容。# 装饰器不带参数就是2层嵌套。def outer(a):  # 第一层 负责接收装饰器的参数    def decorator(func):  # 第二层 负责接收函数的        def wrapper(*args, **kwargs):  # 第三层 负责接收函数的参数            func(*args, **kwargs)            print('---->铺地砖{}块。'.format(a))        return wrapper  # 返出来的是第三层    return decorator  # 返出来的是第二层@outer(a=10)def house(time):    print('我{}日期拿到房子的钥匙，是毛坯房。'.format(time))@outer(100)def walkway():    print('新修的街道：名字是：湖光路')house('2020-05-13')walkway()