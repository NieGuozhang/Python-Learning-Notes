# 魔术方法：普通方法需要调用才会运行，而魔术方法是在特定时刻自动触发。# __init__:初始化魔术方法# 触发时机：初始化对象时触发（不是实例化触发，但是和实例化触发在一个操作中。）# __new__ :实例化的魔术方法# 触发时机：在实例化对象时触发# __call__ : 对象调用方法# 触发时机：将对象当做函数调用时，会默认调用此函数# __del__: 析构魔术方法# 触发时机：class Person:    def __init__(self, name):        self.name = name        print('-------->init', self.name)    def __new__(cls, *args, **kwargs):  # __new__ 向内存申请空间 =》 地址        print('-------->new')        position = object.__new__(cls)        print(position)        return position    # 如果想把对象当做函数调用，需要重写__call__ 方法    def __call__(self, name):        print('--------> call', name)p = Person('java')print(p)p('nie')