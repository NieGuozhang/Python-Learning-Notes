# __del__ : 析构函数'''__del__:    1.对象赋值：        p = Persom()        p1 = p        说明p1和p指向同一个地址    2.删除地址的引用        del p1  删除p1对地址的引用    3.查看地址的引用次数：        import sys        print(sys.getrefcount(p))    4.当一块空间没有了任何引用，默认执行__del__'''import sysclass Person:    def __init__(self, name):        self.name = name    def __del__(self):        print('-------> del')p = Person('jack')# 对象赋值p1 = pprint(p1.name)p2 = pprint(p2.name)p1.name = 'mary'print(p.name)print(p2.name)del p2print('删除p2后', p.name)del p1print('删除p1后', p.name)# del p# print('删除p后，')