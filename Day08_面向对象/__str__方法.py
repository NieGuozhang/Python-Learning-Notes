# __str__# 触发时机：打印对象名，自动触发，去调用__str__里面的内容# 注意：一定要加返回值 return, return后面的内容就是打印对象看到的内容部分。class Person:    def __init__(self, name):        self.name = name    def __str__(self):        return self.namep = Person('tom')print(p)# 单纯打印对象名称，出来的是一个地址。地址对于开发者来说，没有太大意义。# 如果想在打印对象名时候，能够给开发者更多一些信息时。'''总结: 魔术方法重点：__init__ （构造函数, 创建完空间之后，第一个调用的方法。），  __str__ （）了解：__new__ 作用：开辟空间__del__ 作用：没有指针引用的时候，会调用。一般都99%不需要重写它。__call__ 作用: 把对象当做函数使用。    '''