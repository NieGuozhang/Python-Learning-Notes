# 文件操作:'''    文件上传:    保存blog    系统函数：    读：        open(file, mode, buffering, encoding)   ---->返回值 stream(管道)'''stream = open(r'aa.txt')# container = stream.read()# print(container)result = stream.readable() # 判断是否可读 返回：True or Falseprint(result)# while True:#     line = stream.readline()#     print(line)#     if not line:#         break#lines = stream.readlines() # 读取的内容保存在列表中for i in lines:    print(i)stream = open('../Day04_函数/ngz.jpg', 'rb')  # 如果是图片，则不能使用默认的读取方式，应用mode='rbprint(stream.read())