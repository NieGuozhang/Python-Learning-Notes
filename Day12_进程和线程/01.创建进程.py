"""进程>线程>协程进程：是系统进行资源调度的基本单位优点：    稳定性高、一个进程崩溃了，不会影响其他进程（进程和进程之间是独立的）缺点：    创建进程开销大    操作系统能同时运行的进程数目有限"""# 进程创建"""from multiprocessing import Processprocess = Process(target = 任务函数，name = 任务名字， args = (给函数传递的参数,))process对象对象调用方法：process.start() 启动进程并执行任务process.run()只是执行任务，并没有启动进程terminate()终止进程"""from multiprocessing import Processimport timeimport osdef task1():    while True:        time.sleep(1)        print('这是任务1。。。。。。。', os.getpid(), '-' * 5, os.getppid())def task2():    while True:        time.sleep(1)        print('这是任务2。。。。。。。', os.getpid(), '-' * 5, os.getppid())if __name__ == '__main__':    # 子进程    p1 = Process(target=task1, name='任务1')    p1.start()    p2 = Process(target=task2, name='任务2')    p2.start()    # 主进程打印任务    print('-' * 20)    print('-' * 20)    print(os.getpid())