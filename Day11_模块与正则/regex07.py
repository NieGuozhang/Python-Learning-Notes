import remsg = '佟丽娅娜扎热巴戴斯'pattern = re.compile('佟丽娅')result = pattern.match(msg)  # 没有匹配print(result)# 使用正则re模块方法：matchs = '娜扎热巴佟丽娅戴斯'result = re.match('佟丽娅', s)  # 只要从开头进行匹配，如果匹配不成功就返回noneprint(result)result = re.search('佟丽娅', s)  # Search进行正则字符串匹配方法，匹配的是整个字符串,只要找到一个就不匹配后面的了。print(result)print(result.span())  # 返回位置print(result.group())  # 使用group提取匹配到的内容print(result.groups())# []表示的是一个范围s = '哈哈5a'r = re.search('[0-9][a-z]', s)print(r.group())msg = 'abdgcg8ddjbkd07hdn3jj'result = re.search('[a-z][0-9][a-z]', msg)print(result.group())# 正则符号result = re.findall('[a-z][0-9][a-z]', msg)  # findall()是查找字符串所有匹配的，返回一个列表。print(result)# a7a,  a88a, a7878amsg = 'a7aahgahana88aahodhsodba7878a'result = re.findall('[a-z][0-9]+[a-z]', msg)print(result)# QQ号码验证 5~11位 开头不能为0qq = '76152984999'result = re.match('^[1-9][0-9]{4,10}$', qq)print(result)# 用户名可以是字母或者数字，不能是数字开头，用户名长度必须是6位以上 [0-9a-zA-Z]username = 'a001admin'result = re.match('[a-zA-Z]\w{5,}', username)print(result)msg = 'a*py ab.txt bb.py kk.png uu.py'result = re.findall(r'\w*\.py\b', msg)print(result)'''总结：    . 任意字符（除\n)    ^ 开头    $ 结尾    [] 范围 [abc] [a-z*&%]            正则预定义：    \s  空白    \b 边界    \d 数字    \w word [0-9a-zA-Z]        大写反面 \S 非空格    \D 非数字        量词：    * >=0    + >=1    ? 0,1        手机号码的正则：    re.match('1[35789]\d{9}$', )        {m} 固定m位    {m,} >=m位    {m,n] phone>=m, phone<=n        '''