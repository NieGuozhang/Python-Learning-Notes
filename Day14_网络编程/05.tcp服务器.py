"""tcp服务器流程如下：    1、socket创建一个套接字    2、bind绑定IP和port    3、listen设置最大连接数，收到连接请求后，这些请求需要排队，如果队列满，就拒绝请求    4、accept等待客户端的连接、接收连接请求    5、recv/send接收/发送数据"""from socket import *# 1、创建tcp 服务端的sockets = socket(AF_INET, SOCK_STREAM)# 2、绑定端口s.bind(("", 7777))# 3、listen将套接字对象设置为监听状态，并设置最大连接数s.listen(5)# 4、如果有新的客户端来链接服务器，那么就产生一个新的套接字对象专门为这个客户端服务# client_socket 用来为这个客户端服务# s 就可以省下来专门等待其他新客户端的链接client_socket, client_addr = s.accept()# 发送数据client_socket.send('hello, tcp'.encode('gbk'))# 接收数据data = client_socket.recv(1024)print("服务器端接收的数据：", data.decode('gbk'))# 关闭套接字client_socket.close()s.close()